services:
  segmentation:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./configs:/app/configs:ro
      - ./models:/app/models:ro
      - ./data:/app/data:ro
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
